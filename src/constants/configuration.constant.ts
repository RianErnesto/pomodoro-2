import { PomodoroConfigurations } from "@/types/Configuration.type";

export const DEFAULT_CONFIGURATION: PomodoroConfigurations = {
  hideTimeOnTitle: false,
  initialRepeats: 1,
  alwaysStartWithInitialRepeats: true,
  screenSize: "md",
};
